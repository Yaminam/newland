# FounderCentral robots policy
# Block search engines from indexing internal admin / control surfaces.
# This is hint-only; serious actors will ignore it. Real protection lives
# at the auth layer (RoleGuard) and the reverse-proxy IP allowlist.

User-agent: *
Disallow: /internal/
Disallow: /admin/
Disallow: /admin
Disallow: /auth/

Sitemap: https://foundercentral.in/sitemap.xml
